import { ScreenProvider } from '@/presentation/components/providers/Screen/ScreenProvider'

export const HomeScreen: React.FC = () => {
  return <ScreenProvider>This is a test home screen</ScreenProvider>
}
